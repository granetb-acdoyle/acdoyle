{
  "name": "acdoyle",
  "description": "Agent-to-agent gateway. Routes a task to one of three specialist personas (Sherlock, Watson, Moriarty) and settles payment in USDC on Base.",
  "url": "https://acdoyle.dev",
  "repository": "https://github.com/granetb-acdoyle/acdoyle",
  "manifest_version": "1.0",
  "endpoints": {
    "dispatch": {
      "method": "POST",
      "path": "/api/curate",
      "auth": { "type": "header", "header": "x-api-key" },
      "request_body": {
        "query": "string — the task or question, in plain language. The router picks Sherlock, Watson, or Moriarty automatically."
      },
      "response": "Persona-shaped JSON. Sherlock/Moriarty: decisive recommendation plus rationale. Watson: allocation decision log plus computed commission (simulated execution)."
    },
    "topup": {
      "method": "POST",
      "path": "/api/topup",
      "description": "Creates a pending credit top-up intent with a unique expected USDC amount and asfo's receiving wallet address on Base."
    }
  },
  "mcp": {
    "url": "https://acdoyle.dev/api/mcp",
    "transport": "streamable-http",
    "tools": ["acdoyle_dispatch"]
  },
  "payment": {
    "asset": "USDC",
    "chain": "Base",
    "network": "base-sepolia",
    "mechanism": "prepaid-credit",
    "flow": "POST /api/topup for an intent, send the exact USDC amount on Base, an Alchemy webhook detects the transfer and grants credit automatically",
    "pricing": "Single non-itemized total_fee_usdc per /api/curate call. Watson additionally takes a commission (currently 5%) on allocated amounts, computed server-side.",
    "x402": "POST /api/curate/x402 accepts the same request body as /api/curate but requires no api_key — instead it is gated by an x402 payment (scheme 'exact', network eip155:84532 / Base Sepolia) priced per call via the server's X402_PRICE_USD configuration. Pay-per-call, no top-up or credit ledger involved. Base Sepolia testnet only for now."
  },
  "personas": {
    "sherlock": "General-purpose decisive research and recommendation. One confident answer with a one-line justification, never a hedged list.",
    "watson": "Budget planning, non-custodial. Given a budget, decides concrete allocations and instructs the calling agent on exactly what to pay and where — Watson never holds or moves funds itself. Decisions are logged to a real ledger. Commission is currently a flat rate, pending a savings-based model.",
    "moriarty": "Opportunity and monetization advice, as decisive as Sherlock, with a fixed disclaimer appended by the server."
  },
  "status": "pre-revenue, live on Base Sepolia testnet"
}
